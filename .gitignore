# ── Environment & secrets ─────────────────────
.env
.env.*
!.env.example
k8s/secrets.yaml

# ── Hidden folders ────────────────────────────
.hidden/
DevPlan+Notes/

# ── Claude Code instructions ─────────────────
CLAUDE*.md
**/CLAUDE*.md

# ── Error logs ───────────────────────────────
chromebook-error-log/

# ── Python (backend) ─────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.venv/
venv/
env/
.mypy_cache/
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage
.coverage.*

# ── Node.js (frontend) ───────────────────────
node_modules/
.next/
out/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.vercel
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── IDE & OS ─────────────────────────────────
.DS_Store
*.pem
.idea/
.vscode/
*.swp
*.swo

# ── Test artifacts (backend root) ────────────
waterpulse-backend/test_*.py
waterpulse-backend/test_*.json
waterpulse-backend/*_results.json

# ── Large data files ─────────────────────────
/ListStationsAndAlerts.json
Python Component Creation/*.json
Python Component Creation/*.csv

# ── PMTiles basemap (local only — 5–6 GB) ────
/tiles/*.pmtiles

# ── Cloudflare tunnel artifacts ──────────────
# Log files from `cloudflared tunnel --url ...` run in the background
# (see docs/cloudflare-tunnel.md). Keep the guide committed; ignore runtime output.
tunnel.log
*.tunnel.log
# Named-tunnel credentials (if you ever create one via `cloudflared tunnel login`)
.cloudflared/
cloudflared-config.yml
cloudflared-config.yaml
